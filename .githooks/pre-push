#!/bin/bash
# Pre-push gate: mirrors the CI build-test job. Installed via `task hooks`.
# Skip with: git push --no-verify
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

echo "hook: swift build…"
swift build

echo "hook: swift test…"
swift test
