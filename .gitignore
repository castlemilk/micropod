.build/
.swiftpm/
DerivedData/
*.xcodeproj
*.xcworkspace
.DS_Store
dist/
# CI sample working dirs (recreated by the pipelines)
ci/samples/*/node_modules/
# Local tool/env state — never commit
.env
.serena/
.wrangler/
bench/

# Next.js docs explorer
web/node_modules/
web/.next/
web/out/
web/public/specs/

# SDK build artifacts (generated sources ARE committed; .build/ and dist/
# are already global patterns above)
sdk/ts/node_modules/

# Vendored apple/container checkout kept locally for reference
docs/container/
