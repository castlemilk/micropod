# BrandBrain illustration assets

BrandBrain flow-orchestrator asset set (`flow_session_92ae88f95139`), manifest
rev 3 (`sha256:9093b83d…`), live run `d5b8b85c…` (OpenAI gpt-image-2, high,
1536×1024 solid-matte contact sheet). The 14 illustrations were split from the
generated sheet locally (2×7 row-major grid per the manifest layout) and trimmed
to content; each is white-matte navy/blue line art in the Micropod brand style.

Loaded by `EmptyStateView` / `EmptyStateArtwork` (via `Bundle.module`, with
`Bundle.main` fallback) — SF-symbol fallback applies for any missing stem.

Stems: containers-empty, images-empty, volumes-empty, networks-empty,
registries-empty, build-empty, compose-empty, environments-empty,
storage-empty, dashboard-hero, onboarding-runtime, onboarding-kernel,
onboarding-pull, onboarding-run (.png).

Pending: SVG variants (the run's vectorize stage never became fetchable — the
backend run-record path was wedged; drop SVGs here if the gallery recovers).
